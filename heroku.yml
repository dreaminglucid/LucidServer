# heroku.yml

# Setup section for defining add-ons and environment variables
setup:
  addons:
    - plan: heroku-postgresql  # Adding PostgreSQL add-on
      as: DATABASE
  config:
    CLIENT_TYPE: POSTGRES
    POSTGRES_CONNECTION_STRING: postgresql://postgres:Shinonuma33~@db.ewerpfnwpqesorbwelis.supabase.co:5432/postgres

# Build section for defining the Dockerfile for web
build:
  docker:
    web: Dockerfile  # Dockerfile for web process

# Run section for defining the processes to run
run:
  web: bundle exec puma -C config/puma.rb  # Web process command
